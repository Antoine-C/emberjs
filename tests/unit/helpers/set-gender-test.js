import { setGender } from '../../../helpers/set-gender';
import { module, test } from 'qunit';

module('Unit | Helper | set gender');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = setGender(42);
  assert.ok(result);
});
