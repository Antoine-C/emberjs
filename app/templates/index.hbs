<div class="row">
    {{#each item in model}}
        <div class="large-4 medium-4 small-4 columns">
            <div class="callout panel">
                {{item.name}}
            </div>
        </div>
    {{/each}}
</div>
